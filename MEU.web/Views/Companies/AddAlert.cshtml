@model MEU.web.Models.AlertsViewModel
@{
    ViewData["Title"] = "AddAlert";
}

<h2>Add</h2>

<div class="row">
    <div class="col-md-4">
        <form asp-action="AddAlert">
            <input type="hidden" asp-for="Status_id" />

            <partial name="_Alert" />

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>

        </form>
    </div>
</div>